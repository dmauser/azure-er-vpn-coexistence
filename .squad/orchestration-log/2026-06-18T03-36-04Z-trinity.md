# Orchestration Log — Trinity

**Date:** 2026-06-18T03:36:04Z  
**Agent:** Trinity (Azure Terraform Engineer)  
**Session ID:** az-vpn-sku-fix

## Scope

Azure VPN Gateway AZ SKU consolidation fix and Standard public IP gate decision.

## Work Completed

### 1. VPN Gateway AZ SKU Consolidation

**Trigger:** Live deployment error `NonAzSkusNotAllowedForVPNGateway` — Azure no longer supports non-AZ SKUs (VpnGw1-5).

**Changes:**
- `terraform/azure/variables.tf`:
  - Updated `gateway_sku` default from `"VpnGw1"` to `"VpnGw1AZ"`
  - Added validation regex `^VpnGw[1-5]AZ$` to enforce AZ-only SKU selection
  - Updated description to note NonAzSkusNotAllowedForVPNGateway requirement

- `terraform/azure/gateways.tf`:
  - Added `zones = ["1", "2", "3"]` to `azurerm_public_ip.vpn_gw_pip1` (zone-redundant Standard PIP)
  - Added `zones = ["1", "2", "3"]` to `azurerm_public_ip.vpn_gw_pip2` (zone-redundant Standard PIP)
  - AZ SKUs require Standard SKU PIPs with zone redundancy

- `terraform/azure/terraform.tfvars.example`:
  - Changed commented example from `gateway_sku = "VpnGw1"` to `gateway_sku = "VpnGw1AZ"`

- `terraform/README.md`:
  - Added troubleshooting subsection "Azure VPN Gateway: Non-AZ SKU Error"
  - Documents the error message, Azure's AZ-only consolidation, solution (use VpnGw1AZ+), and validation rules

**Verification:**
- Live deployment tested: Azure accepted `VpnGw1AZ` with zone-redundant Standard PIPs
- `terraform validate` passes
- No backward compatibility; users must update or use deploy wrappers with corrected defaults

### 2. Standard Public IP Gate Decision

**Trigger:** DMAUSER-FDPO subscription requires `Microsoft.Network/AllowBringYourOwnPublicIpAddress` feature before Standard PIP allocation. Without it, `terraform apply` fails ~20 minutes into execution at PIP creation with `SubscriptionNotRegisteredForFeature`.

**Decisions:**
- Keep Standard SKU (active-active BGP gateways require it; Basic SKU is retired)
- Do NOT auto-register features (user explicitly does not want automation modifying subscription-level settings)
- Add pre-flight detection + guidance to deploy scripts (detect, explain, provide exact fix commands)

**Changes to deploy scripts (not yet committed at orchestration-log time):**
- `deploy.ps1`: New `Test-Prereqs` step "Azure Standard public IP capability"
- `deploy.sh`: New `check_prereqs` step "Azure Standard public IP capability"
- Both probe with temp RG creation; always clean up; exit with user-friendly guidance on feature registration

**Changes to terraform/README.md:**
- New troubleshooting subsection "Restricted subscriptions: Standard public IP gate"
- Symptom, cause, fix, and SKIP_PIP_PRECHECK env var bypass documented

## Decision Record

**Trinity Decision:** VPN Gateway AZ SKU Consolidation → Status: COMPLETED  
**Trinity Decision:** Standard PIP Gate — Detection + Guidance → Status: COMPLETED

## Outputs

1. **Terraform Config Changes:**
   - Variables: `gateway_sku` default + validation
   - Resources: zone-redundant Standard PIPs on VPN gateways
   - Example tfvars: SKU updated

2. **Documentation:**
   - terraform/README.md: 2 new troubleshooting subsections

3. **Ready for Review:**
   - All changes pass `terraform validate`
   - Live deployment tested and confirmed working
   - Handed to Morpheus for reviewer gate

## Next Steps

- Morpheus review and approval (COMPLETED)
- Scribe: Merge decisions to decisions.md, create orchestration logs, commit squad files
