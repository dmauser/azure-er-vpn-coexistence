# Orchestration Log — Trinity (Azure VM Hardening & Route Dumps)

**Date:** 2026-06-17  
**Time:** 20:47:00 UTC-05:00  
**Agent:** Trinity (🔧 Azure Terraform Engineer)  
**Session:** lab Terraform revamp finalization

## Tasks Completed

### 1. Azure VM Hardening
- Removed 3 public IPs from VM resources (no `public_ip` binding on NICs, no VM-level public IP resources)
- Removed `restrict_ssh_source_prefix` input variable
- Removed NSG inbound SSH rule (only Serial Console access via managed boot diagnostics)
- Added `managed_boot_diagnostics` (enabled by default) to all three Linux VMs
- Enabled cloud-init `custom_data` to install network test tools (`net-tools`, `traceroute`, `tcptraceroute`, `nmap`, `hping3`, `iperf3`, `nginx`, `speedtest-cli`, `moreutils`)

### 2. Azure Route Dump Scripts
- Created `dump-routes-azure.sh` (bash diagnostics helper)
- Created `dump-routes-azure.ps1` (PowerShell diagnostics helper)
- Scripts dump ExpressRoute circuit routes, VM effective routes, and ER gateway learned routes
- Gracefully continue when ExpressRoute disabled or not provisioned

### 3. VM Admin Password Handling
- Applied trap fix in `deploy.sh` to ensure `TF_VAR_vm_admin_password` is cleared on EXIT (addressing Morpheus's High-severity finding)
- Trap: `trap 'unset TF_VAR_vm_admin_password' EXIT`

## Validation

✅ Terraform validate passes: `terraform -chdir="terraform/azure" validate`  
✅ Bash syntax check: `bash -n deploy.sh` passes  
✅ Deploy script trap correctly unsets password on any exit

## Decisions Logged

- **Decision #14:** Trinity — VM Hardening and Azure Route Dumps
  - Private-only Azure VMs (Serial Console access only)
  - Cloud-init network tools installation
  - Route dump scripts added to root level

## Artifacts Staged (by coordinator)

- `terraform/azure/main.tf` — VM hardening, no public IPs
- `terraform/azure/variables.tf` — `restrict_ssh_source_prefix` removed
- `terraform/azure/vm.tf` — cloud-init custom_data added
- `dump-routes-azure.sh` / `dump-routes-azure.ps1` — root level
- `deploy.sh` — trap fix applied

## Notes

- First boot depends on Azure default outbound access for apt (no explicit NAT Gateway/Firewall egress defined)
- Future explicit egress design may be needed if default access is removed
- Tank/Switch: update deployment logic and docs to reflect no `restrict_ssh_source_prefix`, no public IP retrieval, Serial Console access only
