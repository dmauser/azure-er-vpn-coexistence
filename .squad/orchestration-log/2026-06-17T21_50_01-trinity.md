# Orchestration Log — Trinity — 2026-06-17T21:50:01-05:00

## Manifest Outcome

**Status:** SUCCESS

### Assignment

Added pre-flight "Azure Standard public IP capability" probe to `deploy.sh` + `deploy.ps1`:
- Detect the `AllowBringYourOwnPublicIpAddress` Standard-PIP gate before `terraform apply`
- Provide clear fix guidance (three `az feature register/show` commands)
- Support `SKIP_PIP_PRECHECK=1` bypass for known-good subscriptions
- No auto-register (user discretion only)

Added "Restricted subscriptions: Standard public IP gate" documentation section to `terraform/README.md` Troubleshooting.

### Validation Results

- ✅ `bash -n deploy.sh` — Syntax check passed (exit 0)
- ✅ PowerShell AST parse (`deploy.ps1`) — 0 errors
- ✅ `terraform validate` (both `terraform/azure` and `terraform/gcp`) — Success

### Files Modified

- `deploy.sh` — Added Standard-PIP probe in `check_prereqs()` function
- `deploy.ps1` — Added Standard-PIP probe in `Test-Prereqs()` function
- `terraform/README.md` — Added Troubleshooting subsection "Restricted subscriptions: Standard public IP gate"
- `.squad/agents/trinity/history.md` — Appended session entry

### Justification

Active-active VPN gateways with BGP require Standard SKU public IPs. Azure Basic SKU PIPs were retired 2025-09-30. On restricted subscriptions (e.g., DMAUSER-FDPO), Standard PIP allocation is gated behind the feature flag `Microsoft.Network/AllowBringYourOwnPublicIpAddress`. Without early detection, `terraform apply` fails silently ~20 min into the run at the gateway PIP step, confusing users. Pre-flight probe detects this early and provides exact remediation.

### Outcome

✅ **SUCCESS** — Pre-flight probe added, documented, syntax-validated. Trinity history appended.
