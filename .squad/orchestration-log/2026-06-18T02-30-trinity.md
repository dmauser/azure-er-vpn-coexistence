# Orchestration Log — Trinity tfvars Precedence Guard

**Timestamp:** 2026-06-18T02:30:00Z  
**Agent:** Trinity (🔧)  
**Task:** Deploy script guard — fail-fast validation for terraform.tfvars vm_admin_password  

## Summary

Added fail-fast guard to `deploy.sh` and `deploy.ps1` to detect and reject active `vm_admin_password` assignment in `terraform/azure/terraform.tfvars` during prerequisite validation. This prevents Terraform's variable-precedence rule (tfvars > env var) from silently overriding the strong password collected by the deploy wrappers.

## Changes Committed

- `deploy.sh`: Added password placeholder detection in prereq validation, syntax verified with `bash -n`
- `deploy.ps1`: Added password placeholder detection in prereq validation, AST verified with PowerShell parser
- `terraform/azure/terraform.tfvars.example`: Commented out vm_admin_username and vm_admin_password with note explaining script supply behavior

## Verification

- bash -n clean (exit 0)
- PowerShell AST parse clean (0 errors)
- No .tf files modified

## Integration

This change integrates with decision #15 (Azure VM Admin Password Complexity Enforcement) and completes the deploy-wrapper story for password handling. Users are now protected from silent override via variable precedence.

## Next Steps

None — guard is now active in both deploy wrappers and will catch misconfiguration at next user run.
